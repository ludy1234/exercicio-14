/*#include <stdio.h>

  int main() {
    int a, b;

    printf("Digite dois numeros inteiros:\n");
    scanf("%d %d", &a, &b);

    printf("Conteudo com maior endereço: %d\n", (*(&a > &b ? &a : &b)));

    return 0;
  }  */