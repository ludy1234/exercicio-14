/*#include <stdio.h>

  void calcularSoma(int *a, int *b){
    *a = *a + *b;
  }

  int main() {
    int A = 12, B = 10;

    calcularSoma(&A, &B);

    printf("Valor de A: %d\nValor de B: %d\n", A, B);

    return 0;
  }*/