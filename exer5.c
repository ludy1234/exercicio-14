/*#include <stdio.h>

  void encontrarMaiorMenor(int *a, int *b) {
    int temp;
    if (*a < *b) {
    temp = *a;
    *a = *b;
    *b = temp;
  }
}

  int main() {
    int A, B;

    printf("Digite dois numeros inteiros:\n");
    scanf("%d %d", &A, &B);

  encontrarMaiorMenor(&A, &B);

    printf("Maior: %d\nMenor: %d\n", A, B);

    return 0;
  }*/